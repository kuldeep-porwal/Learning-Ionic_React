import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'LearningIonicReact',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
